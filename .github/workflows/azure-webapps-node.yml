name: VPS-Desktop-Final
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Launch Desktop
        run: |
          wget -qO- https://raw.githubusercontent.com/alireza0/vps-desktop/main/setup.sh | bash
