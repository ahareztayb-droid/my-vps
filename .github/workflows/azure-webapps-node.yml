name: My-Powerful-VPS
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Start Tunnel
        run: |
          curl -sL https://git.io/file-transfer | sh
          ./transfer --help
